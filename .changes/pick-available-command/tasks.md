# Tasks — pick an available command

## Behavioral
- [ ] B1  Snapshot only Available Commands                    → behavior.md §1
- [ ] B2  Rebuild command Candidates for every opening       → behavior.md §2
- [ ] B3  Disable the Source when the API is unavailable     → behavior.md §3
- [ ] B4  Narrow commands by Candidate Name                  → behavior.md §4
- [ ] B5  Show the no-match state                            → behavior.md §5
- [ ] B6  Bound basic navigation                             → behavior.md §6
- [ ] B7  Select after teardown                              → behavior.md §7
- [ ] B8  Report command execution failure                   → behavior.md §8
- [ ] B9  Cancel without execution                           → behavior.md §9
- [ ] B10 Replace the existing Picker                        → behavior.md §10

## Chores
- [ ] C1  Replace sample manifest, package, README, lifecycle, settings, and styles with Vertico identity and the minimal Picker shell
- [ ] C2  Add the `node:test` runner, test script, and `fzf` runtime dependency
- [ ] C3  Register stable `pick-command` with no default hotkey and disposable lifecycle wiring
- [ ] C4  Verify the minimum Obsidian version used and keep `manifest.json` and `versions.json` aligned
- [ ] C5  Run `npm test`, `npm run build`, and `npm run lint`; manually demo selection, cancellation, replacement, and the unsupported-API Notice in desktop Obsidian
