# Target desktop Obsidian only

The plugin targets desktop Obsidian because its dense, keyboard-driven Picker is intended for power-user workflows where the stock mobile switcher is sufficient. This is a product boundary rather than a reason to adopt native dependencies; dependencies must still justify their cost independently.
